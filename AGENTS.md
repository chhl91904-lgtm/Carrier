# CANE MATE 프로젝트 작업 지침

## 1. 문서의 목적과 우선순위

이 파일은 CANE MATE 공식 브랜드 웹사이트를 설계·구현·검증하는 모든 작업자가 지켜야 할 최상위 프로젝트 지침이다. 구현 전에 `docs/SITE_SPEC.md`와 `docs/CHECKLIST.md`를 함께 읽는다.

지침이 충돌할 때의 우선순위는 다음과 같다.

1. 사용자가 현재 대화에서 명시한 최신 요구사항
2. 이 `AGENTS.md`의 변경 금지 원칙
3. `docs/SITE_SPEC.md`의 기능·콘텐츠·상태 명세
4. `docs/CHECKLIST.md`의 단계와 테스트 게이트
5. 기존 코드의 관례

요구사항을 해석하기 어렵거나 중요한 기술 선택에 따라 결과가 크게 달라질 때는 임의로 범위를 줄이지 말고 사용자에게 확인한다.

## 2. 프로젝트 목표

CANE MATE는 폐에어컨에서 회수·재생한 소재와 스마트 기술을 활용해 시각장애인의 더 안전하고 편리한 이동을 보조하는 스마트 흰지팡이 프로젝트다.

웹사이트는 단순 홍보용 랜딩 페이지가 아니라 다음을 포괄하는 현실적인 기업 공식 브랜드·서비스 사이트로 만든다.

- 브랜드와 제품의 목적 전달
- 제품 기능·내부 구조·기획 사양·앱 연동 소개
- 제품 구매, 선물, 장바구니, 쿠폰, 데모 주문
- 체험존, 체험 프로그램, 피드백, 5% 체험 쿠폰
- 외부 협업 서비스인 쉼터MAP 연계
- ESG의 환경·사회·거버넌스 가치 설명
- 사용 가이드, FAQ, 문의, A/S
- 회원가입, 로그인, 마이페이지, 주문·쿠폰·후기·문의 내역
- 모바일·태블릿·PC 반응형과 웹 접근성

## 3. 절대 변경 금지 원칙

다음 항목은 사용자 승인 없이 추가·삭제·축소·왜곡하지 않는다.

1. CANE MATE의 주요 기능과 페이지 범위
2. 흰색 긴 shaft, 검은색 ㄱ자형 Smart Handle, Wrist Strap, 4단 접이식, 검은색 Cane Tip, 전면 소형 Dual-Camera Style ToF Sensor, 버튼, 내부 Smart Core, 송풍·온열 구조로 정의된 제품 디자인
3. 사용자 제공 제품 이미지가 있을 경우 해당 자산을 최우선으로 사용하는 원칙
4. CANE MATE를 의료기기, 안내견의 완전한 대체품, 자율주행 지팡이로 표현하지 않는 원칙
5. 낙상 위험 감지를 100% 정확하거나 자동 신고가 보장되는 기능으로 표현하지 않는 원칙
6. 미검증 ESG 성과, 탄소 감축량, 성능, 배터리 시간, 상용 사양을 만들어 내지 않는 원칙
7. 시각장애인을 수동적·불쌍한 대상으로 묘사하지 않고 독립적인 이동을 보조하는 제품으로 표현하는 원칙
8. 쉼터MAP을 CANE MATE 자체 서비스가 아닌 외부 협업 서비스로 표시하는 원칙
9. 개인정보와 위치정보를 최소 수집·동의 기반으로 다루는 원칙
10. 완료·검증된 기능을 이후 단계에서 사용자 승인 없이 제거하거나 의미를 변경하지 않는 원칙
11. 실제 결제가 연결되지 않은 상태를 결제 완료로 오인시키지 않는 원칙
12. 접근성 체험을 장애인의 불편을 구경하거나 모방하는 프로그램으로 표현하지 않는 원칙

## 4. 제품 및 콘텐츠 표현 원칙

- 제품은 안전한 이동을 **보조**하는 스마트 흰지팡이로 설명한다.
- ToF는 일반 흰지팡이로 놓치기 쉬운 전방의 높은 장애물을 보조 감지하는 센서로 설명한다.
- 약 40~45° 사용 각도, 약 125cm 길이, 접었을 때 약 33cm, 3,000mAh, 약 11.1Wh, 약 350g, 149,000원은 모두 현재 **기획 기준·목표치**임을 표시한다.
- 배터리 사용 시간 등 제공되지 않은 수치는 작성하지 않는다.
- 햅틱은 주로 장애물 위험 알림에 사용하고 길 안내는 스마트폰·이어폰을 통한 음성을 중심으로 설명한다.
- 낙상 의심 시 보호자에게 확인 알림을 보내는 흐름으로 설명하며 즉시 자동 신고로 표현하지 않는다.
- 위치 확인은 이용자와 보호자의 명시적 동의가 있는 경우로 제한한다.
- 손잡이 송풍은 강력한 냉방이 아닌 소형 팬의 가벼운 바람이며, 온열은 손잡이 온열 필름으로 불편을 줄이는 기능이다.
- 실제 기관 연락처, 주소, 이메일, 외부 URL은 검증된 값만 사용한다. 미제공 값은 명확한 placeholder 또는 중앙 설정값으로 유지한다.
- 실제 상용화·인증·검증이 완료됐다고 암시하지 않는다.

## 5. 디자인 원칙

브랜드 인상은 `Clean / Premium / Accessible / Trustworthy / Technology / Sustainability / Minimal`이다.

- 기본 색상: White, Black/Dark Charcoal, Navy, Carrier Blue 계열
- 현실적인 한국 기업 기술 브랜드의 절제된 시각 언어를 사용한다.
- 과도한 네온, 홀로그램, 우주·로봇 분위기 등 SF 연출을 피한다.
- 모든 화면에서 제품의 형태, 센서 크기, 소재 표현을 일관되게 유지한다.
- 제품 이미지는 핵심 시각 요소로 충분히 크게 사용하되 콘텐츠와 접근성을 침해하지 않는다.
- 상태는 색상만으로 구분하지 않고 텍스트·아이콘·형태를 함께 사용한다.
- 애니메이션은 정보 이해를 돕는 수준으로 제한하고 `prefers-reduced-motion`을 존중한다.
- 모바일, 태블릿, PC에서 정보 우선순위와 조작 가능성을 유지한다.
- 상단 primary navigation은 `PRODUCT | EXPERIENCE | ESG | BUY | SUPPORT` 순서를 최종 기준으로 사용한다.

## 6. 접근성 원칙

접근성은 마지막 보정 작업이 아니라 모든 STEP의 완료 조건이다. 목표 기준은 WCAG 2.2 AA 수준을 기본으로 하며, 검증 가능한 항목은 자동·수동 테스트를 함께 수행한다.

- 의미에 맞는 semantic HTML과 올바른 heading 순서를 사용한다.
- 모든 기능은 키보드만으로 사용 가능해야 한다.
- 포커스가 보이고 논리적인 순서로 이동해야 한다.
- 모든 입력에는 화면에 보이는 label, 오류 설명, 수정 방법을 제공한다.
- 이미지에는 목적에 맞는 대체 텍스트를 제공하고 장식 이미지는 보조기기에서 제외한다.
- 아이콘 단독 버튼에는 명확한 접근 가능한 이름을 제공한다.
- 장바구니 수량, 오류, 쿠폰 적용, 로그인 상태 등 동적 변경은 필요한 경우 screen reader에 전달한다.
- 최소 대비를 충족하고 작은 본문 글씨와 지나치게 긴 행을 피한다.
- 터치 영역은 충분히 크게 만들며 hover에만 의존하지 않는다.
- 모달·드로어는 포커스 이동, 포커스 가두기, Escape 닫기, 닫은 후 포커스 복귀를 처리한다.
- 360° 제품 뷰는 키보드 조작과 정적 대체 이미지를 제공한다.
- 외부 링크는 목적과 외부 이동 여부를 명확히 알린다.

## 7. 기술 및 데이터 원칙

최종 기술 스택과 영속 백엔드는 사용자 승인 후 확정한다. 승인 전 문서의 기준 아키텍처는 다음과 같다.

- TypeScript 기반 컴포넌트형 반응형 웹
- 페이지·도메인·데이터 접근 계층을 분리
- 인증, 카트, 쿠폰, 주문, 후기, 문의를 교체 가능한 repository/service 인터페이스 뒤에 배치
- 외부 쉼터MAP URL, 기관 정보, 기능 플래그를 코드 여러 곳에 하드코딩하지 않고 중앙 설정에서 관리
- 데모 모드에서는 실제 결제·실제 개인정보 수집·실제 위치 추적을 하지 않음
- 운영 연동 시 서버 측 검증, 권한 검사, 개인정보 보존·삭제 정책, 비밀정보 관리, 감사 로그를 추가
- 사용자 입력은 클라이언트와 서버 양쪽에서 검증하고 출력 시 안전하게 처리
- 인증 상태와 소유권을 URL이나 클라이언트 값만 신뢰하지 않음
- 가격, 할인, 합계는 단일 계산 규칙을 사용하며 표시용 값과 계산값을 분리하지 않음
- 주문 스냅샷에는 당시 가격·할인·배송 정보가 보존되어야 함
- 테스트용 데이터는 명백히 샘플임을 표시하고 실존 개인의 정보처럼 보이는 민감 데이터를 만들지 않음

## 8. 작업 절차

1. 시작 전에 `docs/SITE_SPEC.md`에서 해당 기능과 상태를 확인한다.
2. `docs/CHECKLIST.md`에서 현재 STEP만 `in progress`로 보고 해당 범위만 구현한다.
3. 기존 완료 기능과 사용자 변경사항을 보존한다.
4. 구현 후 해당 STEP의 기능·접근성·반응형·회귀 테스트를 실행한다.
5. 실패가 있으면 다음 STEP으로 넘어가지 않는다.
6. 모든 완료 조건을 충족한 항목만 `[x]`로 바꾼다.
7. 사용자에게 결과와 테스트 근거를 보고하고 다음 STEP 진행 승인을 기다린다.

단계 병합, 순서 변경, 범위 축소가 필요하면 이유와 영향을 먼저 설명하고 승인받는다.

## 9. 파일·코드 품질 원칙

- 반복되는 Header, Navigation, Footer, Floating Shelter Button은 공통 컴포넌트로 구현한다.
- 콘텐츠, 제품 사양, 기관 정보, 링크, 가격·할인 정책은 가능한 한 구조화된 데이터 또는 설정으로 관리한다.
- 페이지 컴포넌트에 인증·가격 계산·쿠폰 정책을 직접 섞지 않는다.
- 이름은 도메인 의미가 드러나도록 작성하고 불명확한 축약을 피한다.
- 오류·빈 상태·로딩 상태·권한 없음·외부 링크 미설정 상태를 정상 흐름과 함께 설계한다.
- 콘솔 오류, 깨진 내부 링크, placeholder의 운영 노출을 방치하지 않는다.
- 실제 자산을 받기 전 임시 자산은 `placeholder`임을 명시하고 향후 한 곳에서 교체할 수 있게 한다.
- 환경 변수와 비밀정보를 저장소에 커밋하지 않는다.

## 10. Git 및 변경 관리

- 한 STEP의 변경은 해당 STEP의 목적에 맞게 작고 검토 가능하게 유지한다.
- 사용자 작업과 무관한 파일을 수정하거나 되돌리지 않는다.
- 강제 reset, 광범위 삭제, 비밀정보 커밋을 금지한다.
- 커밋 전 테스트 결과와 변경 범위를 확인한다.
- GitHub 및 배포 작업은 사용자가 해당 STEP을 승인한 뒤 수행한다.

## 11. 구현 전 승인 필요 항목

다음 항목은 문서 작성 시 임의로 확정하지 않는다.

- 프런트엔드 프레임워크와 배포 대상
- 회원·주문·쿠폰·문의 데이터가 브라우저 데모인지 실제 백엔드 영속 데이터인지
- 실제 companion app/BLE 연동 구현 여부(웹사이트 명세의 기본 범위는 앱 연동 **소개**)
- 쉼터MAP 실제 URL과 상호 링크 방식
- Carrier/CANE MATE 로고, 제품 렌더, Exploded View, 360° 프레임 등 공식 자산
- 광주인력개발원·오텍캐리어의 검증된 주소·전화번호·이메일
- 개인정보처리방침, 이용약관, 마케팅 동의 등 실제 법무 문서
- 결제 대행사, 배송, 환불, 쿠폰 만료 기간 등 운영 정책
